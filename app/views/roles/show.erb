role show page
Show all role attributes
tasks to be completed
tasks completed
notes - make sure private has an if statement to check
if the user is logged into that account
<%= @role.id %>
<%= @role.learned? %>

<% if current_user == @role.user %>
    <a href="/roles/<%= @role.id %>/edit">Update Role</a>
    <form action="/roles/<%= @role.id %>" method="POST">
        <input type="hidden" name="_method" value="DELETE">
        <input type="submit" value="Delete Role">
    </form>
<% end %>